{
  "database": {
    "rules": "database.rules.json"
  },
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  },
  "functions": {
    "predeploy": [
      "cd web; yarn build"
    ]
  },
  "hosting": {
    "public": "web/dist"
  },
  "storage": {
    "rules": "storage.rules"
  }
}
